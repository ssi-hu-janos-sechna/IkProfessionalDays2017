spring:
  application:
    name: ${project.name}
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ssi-hu-janos-sechna/IkProfessionalDays2017Config
          searchPaths: '{application}'
logging:
  level:
    root: warn
